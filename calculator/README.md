# Calculator

A compact browser calculator with keyboard support and extended operators.

## Features

- Standard arithmetic operations.
- Modulo and logarithm support.
- Keyboard input for numbers, operators, Enter, Backspace, Escape, and `L` for log.
- Responsive layout that opens directly in the browser.

## Run

Open `index.html` in a browser.
